<config>

    <interface hosts="zonalController* lidar* camera* infotainment adas connectivityGateway" names="eth0" address="192.168.x.x" /> <!-- data plane -->
    <route hosts="zonalController* lidar* camera* infotainment adas connectivityGateway" destination="*" interface="eth0" metric="50"/>
    
    <interface hosts="switch* controller*" names="eth*" address="10.x.x.x"/> <!-- control plane -->
    
</config>
